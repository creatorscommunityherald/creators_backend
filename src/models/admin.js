import mongoose from "mongoose";

let adminSchema = new mongoose.Schema({ //just a mock will code 
    admin_name :String,
    
})

export const admin = new mongoose.model(adminSchema);





/** ********************************ROCKETMAN***************************** */